# @agenttape/test-runner

Agent behavior regression test runner.

Runs baseline tapes from `agent-tests/`, performs offline replay checks, records current runs, diffs baseline vs current, and reports pass/fail.

Supports baseline updates via `--update-baseline`.
