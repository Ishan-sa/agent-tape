# @agenttape/cli

AgentTape command-line interface.

Implemented commands:
- `agenttape record`
- `agenttape replay`
- `agenttape diff`
- `agenttape test`
- `agenttape event`

`record` supports session mode:
- `agenttape record --session --agent "..."`
